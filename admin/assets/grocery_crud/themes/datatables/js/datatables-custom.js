

$(document).ready(function() {

	

	// var search_values = localStorage.getItem('datatables_search_'+ unique_hash);

	// if( search_values !== null)
	// {
	

	// 	if($('#custom_search_settlement_status').length){
	// 		// console.log();
	// 		$("#custom_search_settlement_status").val($.parseJSON(search_values)[4]);
	// 	}
	// }

	
	// $(document).on('change', '.search_settlement_status', function(event) {
	// 	// event.preventDefault();
	// 	// alert($(this).val());
	// 		chosen_table = datatables_get_chosen_table($('.groceryCrudTable'));

	// 		chosen_table.fnFilter( $(this).val(), 4 );

	// 		$('.groceryCrudTable').find('.search_settlement_status').val($(this).val());

	// 		if(use_storage)
	// 		{
	// 			var search_values_array = [];

	// 			chosen_table.find("tfoot tr th").each(function(index,value){
	// 				search_values_array[index] = $(this).children(':first').val();
	// 			});

	// 			localStorage.setItem( 'datatables_search_'+ unique_hash ,'["' + search_values_array.join('","') + '"]');
	// 		}

	// });

	// $('#reportrange').on('apply.daterangepicker', function(ev, picker) {
	//   	console.log(picker.startDate.format('YYYY-MM-DD'));
	//   	console.log(picker.endDate.format('YYYY-MM-DD'));


	//   	chosen_table = datatables_get_chosen_table($('.groceryCrudTable'));

	// 	chosen_table.fnFilter( $(this).val(), 4 );

	// 	$('.groceryCrudTable').find('.search_settlement_status').val($(this).val());

	// 	if(use_storage)
	// 	{
	// 		var search_values_array = [];

	// 		chosen_table.find("tfoot tr th").each(function(index,value){
	// 			search_values_array[index] = $(this).children(':first').val();
	// 		});

	// 		localStorage.setItem( 'datatables_search_'+ unique_hash ,'["' + search_values_array.join('","') + '"]');
	// 	}
	// });




});